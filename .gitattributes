# Set default behavior to automatically normalize line endings
* text=auto

# Force LF for all Python files
*.py text eol=lf

# Optional: force LF for other types if desired
*.sh text eol=lf
*.md text eol=lf
